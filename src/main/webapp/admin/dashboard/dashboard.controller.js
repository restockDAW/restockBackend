app.controller("dashboardCtrl", function($scope, $http, $window, Auth) {
        
    this.$onInit = onInit();
    
    function onInit() {
        console.log("Dashboard controller initiatied!");
    }
        
    
    
});