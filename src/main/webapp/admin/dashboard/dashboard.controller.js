app.controller("dashboardCtrl", function($scope, $http, $window) {
        
    this.$onInit = onInit();
    
    function onInit() {
        console.log("Dashboard controller initiatied!");
    }
        
    
    
});